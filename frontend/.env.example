# Frontend has no secrets — all API calls proxy through the backend
# The backend URL is configured via the "proxy" field in package.json
# If deploying to production, set REACT_APP_API_URL here
REACT_APP_API_URL=http://localhost:5000/api
